[package]
name = "izel_playground"
version = "0.1.0"
edition = "2021"
description = "WASM bridge for the Izel browser playground"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
