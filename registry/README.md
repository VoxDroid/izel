# Izel Public Package Registry Seed

This directory contains the public registry seed index used by Phase 7 bootstrapping.

## Layout

- `index/packages.json`: bootstrap package index metadata.

## Notes

- The package manager defaults to `https://registry.izel.dev` endpoints for index, API, and crate downloads.
- This seed index exists for local evolution and tests while remote registry services are hardened.
