# Izel API Reference

This directory is the canonical landing point for generated and curated API documentation.

Current status:
- Generator integration is in progress.
- Core language and standard library references are still authored under `docs/spec/`.

Planned contents:
- Package-level API indexes.
- Standard library symbol pages.
- Cross-linked references from `izel doc` output.

Until full automation is wired in, use the following sources:
- `docs/spec/appendix-c-stdlib.md` for standard library reference coverage.
- `docs/book/` for narrative guidance and examples.
